# 열린AIT 보안 배포 안내

## 반드시 지킬 원칙

- Gemini API 키를 HTML, JavaScript, GitHub 저장소에 넣지 않습니다.
- 폐기한 키는 다시 사용하지 않습니다.
- 새 키는 배포 서비스의 Secret으로만 저장합니다.

## 필요한 비밀 설정

- `GEMINI_API_KEY`: 새 Gemini API 키
- `ACADEMY_ACCESS_CODE`: 학생에게 안내할 학원 이용 코드

## 일반 설정

- `ALLOWED_ORIGINS`: 실제 앱 주소. 여러 주소는 쉼표로 구분합니다.
- `GEMINI_MODEL`: 기본값 `gemini-3.5-flash-lite`

## 배포 전 확인

1. 저장소에서 `API_KEY`, `AIza`, `AQ.` 문자열이 검색되지 않아야 합니다.
2. 새 키는 Secret 설정 화면에서만 입력합니다.
3. `ALLOWED_ORIGINS`를 실제 서비스 주소와 정확히 맞춥니다.
4. 학원 이용 코드는 API 키와 다른 값으로 정하고 정기적으로 변경합니다.
5. 공개 전 서비스 제공자의 요청 횟수 제한 기능을 활성화합니다.
6. 보안 서버 배포 후 `config.js`의 `YEOLLIN_API_URL`에 발급된 `/api/analyze` 주소를 입력합니다. 이 주소는 비밀정보가 아닙니다.

## 개인정보 운영 원칙

- 학생에게 문제 부분만 촬영하도록 안내합니다.
- 이름, 얼굴, 학교명, 전화번호가 포함된 사진을 올리지 않도록 교육합니다.
- 서버에서 원본 사진이나 AI 답변을 저장하지 않습니다.
- 이용 현황은 사진이나 문제 내용이 아닌 날짜별 호출 횟수만 집계합니다.
