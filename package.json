{
  "name": "yeollin-ait",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check app.js && node --check sw.js && node --check backend/worker.js",
    "dev:api": "wrangler dev",
    "deploy:api": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
